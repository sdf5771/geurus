# 작업 이력 — 2026-09

## 2026-09-15 · PM 초기 세팅

- docs 3종(정본 v2.5·PM 브리프·디자인 발주서) 파악, `agent-log.md`·`README.md` 작성
- Claude Design MCP 인증 필요 → 사용자가 `/design-login` 후 export를 `docs/design/`에 직접 넣음
- 사용자 지시로 `docs/design/` 5종만 남기고 정리(나머지 60개 휴지통), 파일명 영문화
- EXPR 원본 버전 차이 실측 → 키별 원본 지정 ([design.md](../design.md))

## 2026-09-15 · 마일스톤 1 오버레이 셸 — 브랜치 `feat/m1-overlay-shell`

### 구현

| 커밋 | 담당 | 내용 |
|---|---|---|
| `3e709cc` | backend | Vite·Electron·React 스캐폴드 (단독 빌드 실패 — 스쿼시 머지 권장) |
| `28ec95e` | backend | 클릭 통과 IPC 계약·preload (단독 빌드 실패) |
| `86bd805` | backend | 투명 오버레이 창·새싹 트레이 |
| `dbb73f2` | backend | 글리프 원본 경로·위치 비영속 주석 |
| `8e044a2` | frontend | 스프라이트 데이터 이식·합성 |
| `6d830dd` | frontend | 배율별 아틀라스 래스터화 |
| `428edb8` | frontend | 정지 렌더·히트박스 |

### 1차 검증 (qa · review · vision 병렬)

- **vision**: 이탈 없음 — 원본 독립 추출 대조 일치, PNG 바이트 동일
- **review**: 수정 후 Ready — Major 1 / Minor 7 / Nit 6
- **qa**: 하네스 자동 검증 통과 + 버그 B1(= review Major-1, 재로드·크래시 뒤 클릭 통과 고착) · B2(실행 직후 포커스 탈취). 수동 체크리스트 `docs/qa/M1_MANUAL_CHECKLIST.md` 작성

### 수정 라운드 1 (backend·frontend 병렬)

| 커밋 | 내용 |
|---|---|
| `e06b906` | frontend — 마운트 시 `sync()`, 브리지 부재 dev 경고 |
| `059c1e9` | frontend — 순서 테스트(겹치는 가짜 패치), `composeGrid` 112조합 원본 스냅샷, `scripts/snapshot-sprite-original.mjs` |
| `eeec9d5` | frontend — pose·hitbox 주석 |
| `745ad4d` | backend — `overlayRecovery.ts` 클릭 통과 복구 |
| `52c07a3` | backend — `showInactive`, 탐색·새 창 차단, CSP, 단일 인스턴스, 앱 메뉴 제거, renderer 플러그인 제거, preload `.cjs` |
| `f570f14` | backend — .gitignore |

- review 재리뷰: Ready (신규 추정 M-A·M-B) · qa 재검증: B1·B2 통과, 신규 N1
- qa 추가 실측(수정 전): M-A 4/4 재현, M-B 5/10 재현

### 수정 라운드 2 (backend 단독)

| 커밋 | 내용 |
|---|---|
| `1f2a8ed` | N1+M-A(복귀를 `did-navigate`로, `isAllowedNavigation`) · M-B(dev 잠금 생략) · 단조 시계 · editMenu 주석 |
| `f918bc3` | CSP 축소·삽입 위치 보정 |
| `2534cd7` | `snapshot:check` 스크립트·`!.env.example` |

### 최종 검증

- **review**: **Ready** — 전 항목 해소, Blocker/Major/Minor 0. 조건 `docs/design` 동봉
- **qa**: **1~6 전부 통과, 새 버그 없음** — N1 · M-A(빌드·dev reload, 저장 반영) · B1(main reload·크래시 4회) · M-B 7/7 · 빠른 회귀 · 종료 직후 재실행 6/6
- 최종 수치: test 240 · typecheck·build·`snapshot:check` 통과

### 머지 (PM 위임 후)

| PR | 방식 | 결과 |
|---|---|---|
| **#1 마일스톤 1 — 오버레이 셸** | 스쿼시 + 브랜치 삭제 | main `8aa0443` [2026-09-16]. 머지 후 main에서 test 240·build 재확인 |
| **#2 docs: 정본 v2.6** | 스쿼시 + 브랜치 삭제 | main `7c37131` [2026-09-16]. 실측 정정·LEAF 적용 계약·파일 경로 갱신 |

### 2026-09-16 · Electron 버전 검토 (infra)

- 권고: **지금(2단계 전) 30.5.1 → 44.4.1**. 상세·근거·검증 순서는 [stack.md](../stack.md)
- PM이 앞서 "audit 7건, high 2"로 가볍게 요약한 것을 infra가 **critical 1·high 3·moderate 3**으로 정정
- PM의 기존 의견(5단계로 미루기)은 **철회**. 이유: 자동 테스트가 Electron 업그레이드에 신호를 주지 못해 검증 수단이 수동 체크리스트뿐인데, 미루면 그것을 두 번 태워야 함
- 사용자 판단 대기 중. 사전 조건은 **Node 24 LTS 설치**

### 후속 백로그 (1단계 비영향 — PM 판단으로 이 PR에서 수정 라운드 열지 않음)

- **Nit-A** (추정): 에러 페이지 커밋은 `did-navigate` 대신 `did-fail-load` → dev 서버 무응답 full reload 시 ignore=false 잔존 가능 → `did-fail-load(isMainFrame)` 복귀 + 테스트
- **Nit-B**: `devOrigin`이 opaque(`"null"`)면 file:/data: 전부 허용(`VITE_DEV_SERVER_URL` 오설정 시만) → `origin !== 'null'` 가드
- **Nit-C**: 서버 리다이렉트는 `will-navigate` 미경유 → 외부 콘텐츠 로드 단계에서 `will-redirect` 동일 판정
- **Nit-D**: 플러그인이 charset 앞에 리소스 태그를 넣으면 CSP 누락 미감지 → 빌드 실패 가드
- **Nit-1**: spawn 배율 전환 시 앵커 y 튐 → 3단계 전 검토
- qa 하네스(`scratchpad/harness/main2.mjs`)를 `scripts/qa/overlay-smoke.mjs`로 편입할지 — 보류(로컬 전용)
